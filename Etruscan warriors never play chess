#include <stdio.h>
#include <math.h>
#include<iostream>
using namespace std;
int main() {
    int t;
    long long n;
    cin >> t;
    while(t--) {
        cin >> n;
        cout << (long long)floor(sqrt( 2.0 * n + 0.25 ) + 0.5) - 1 <<endl;
    }
    return 0;
}
